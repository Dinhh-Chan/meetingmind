export enum BotSessionStatus {
    QUEUED = "queued",
    JOINING = "joining",
    WAITING_ADMISSION = "waiting_admission",
    RECORDING = "recording",
    LEFT = "left",
    FAILED = "failed",
    CANCELLED = "cancelled",
}
