export enum MediaFileKind {
    AUDIO = "audio",
    ATTACHMENT = "attachment",
    EXPORT = "export",
}

export enum MediaFileStatus {
    UPLOADING = "uploading",
    READY = "ready",
    FAILED = "failed",
    DELETED = "deleted",
}
