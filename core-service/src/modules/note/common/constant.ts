export enum NoteType {
    INFO = "info",
    QUESTION = "question",
    IDEA = "idea",
    DECISION = "decision",
    ACTION = "action",
    CLARIFY = "clarify",
}

export enum NoteVisibility {
    PRIVATE = "private",
    SHARED = "shared",
}
