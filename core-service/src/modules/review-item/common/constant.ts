export enum ReviewItemType {
    ACTION_ITEM = "action_item",
    DECISION = "decision",
    OPEN_ISSUE = "open_issue",
}

export enum ReviewItemStatus {
    PENDING = "pending",
    APPROVED = "approved",
    REJECTED = "rejected",
}
